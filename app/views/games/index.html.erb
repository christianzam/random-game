<div class="h-screen px-5">
  <div class="grid grid-cols-1 md:grid-cols-4 grid-rows-4 md:grid-rows-4 gap-4 h-5/6 w-4/5 mx-auto">
    <% @games.each_with_index do |game| %>
      <div class="max-w-sm p-6 bg-white border border-gray-200 rounded-lg shadow dark:bg-gray-800 dark:border-gray-700">
        <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white"><%= game.date.strftime("%a #{game.date.day.ordinalize}/%b/%Y") %></h5>
        <hr class="my-3">
        <p class="mb-3"> 
         <strong>Torneo:</strong> <%= game&.tournament&.name %>
        </p>
        <div class="flex items-center space-x-4">
          <div class="flex-1 min-w-0">
            <strong>Ganador 🥇:</strong> <%= game.game_result&.winner %>
          </div>
          <div class="inline-flex items-center">
            <%= user_profile_pic(User.last, class_name='w-8 h-8 rounded-full') %>
          </div>
        </div>
        <p class="my-3 font-normal text-gray-700 dark:text-gray-400"> <%= game.player_game_results.size %> Jugadores</p>
        <%= link_to game_path(game), class: "inline-flex items-center px-3 py-2 text-sm font-medium text-center text-white bg-blue-700 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800" do %>
          Detalles
          <svg class="w-3.5 h-3.5 ml-2" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 10">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
          </svg>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
